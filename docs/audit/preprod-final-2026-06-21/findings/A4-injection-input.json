{
  "dimension": "A4-injection-input",
  "summary": "Audited every raw-SQL call site in apps/api (loans, reservations, staff, members, storage, plans, help, admin/fleet, platform/health, export, import, customization/quota), the custom-field ReDoS guard + all save paths, storage path handling, the OpenLibrary SSRF surface, and the xlsx/MARC parsers. SQL is uniformly parameterized (Prisma tagged templates; $queryRawUnsafe uses $1..$N placeholders or constant strings), SSRF is well-defended (pinned host, digits-only ISBN, redirect:'manual', 5s timeout, 1MB cap), local storage blocks traversal (safeResolve rejects .., empty segments, \\0 and verifies root prefix), pg_dump uses execFile with array args + env-passed creds (no shell, no argv credential leak), and MARCXML uses fast-xml-parser (no DTD/external-entity resolution → no XXE). The one real, high-impact defect: the admin-authored regex ReDoS guard (patternLooksCatastrophic) is bypassable by bounded {n} repetition of an ambiguous group — empirically verified to freeze the shared event loop — defeating the very protection it claims to provide.",
  "findings": [
    {
      "severity": "high",
      "title": "ReDoS guard bypassed by bounded {n} repetition of an ambiguous group — platform-wide event-loop freeze",
      "file": "apps/api/src/customization/field-types.ts:126",
      "evidence": "hasAmbiguousRepeatedGroup only treats a group as 'repeated' when followed by * , + , or an OPEN-ENDED brace: `const repeated = next === '*' || next === '+' || (next === '{' && isOpenEndedBrace(p, i + 1));` and isOpenEndedBrace requires a comma: `return /^\\{\\d*,\\d*\\}/.test(p.slice(at));` (field-types.ts:134-135). So a fixed-count brace `{n}` is NOT considered repetition, and a nested-quantifier group repeated a fixed number of times slips past. patternLooksCatastrophic's other gates don't catch it either (length<200, no stacked quantifiers, countUnbounded<=6). Empirically verified against the exact guard code reimplemented from this file: pattern `(a*){5}c` PASSES the guard and `re.test('a'.repeat(40)+'!')` took 319ms; `([a-z]*){8}$` PASSES and never finished within 12s on a 35-char input (exponential). Probe output: `PASSES (a*){5}c  50:1047.0ms  100:10272.8ms ABORT(>4s)` and `PASSES ([a-z]*){8}$  50:64243.2ms ABORT(>4s)`.",
      "impact": "An owner/admin saves a malicious validation pattern (e.g. `([a-z]*){8}$`) on any built-in custom field (book/member/loan/reservation/book_copy) or collection field — patternSaveError accepts it on all four write paths. Thereafter ANY user who creates/updates such a record and supplies a ~40-char value for that field reaches validateField (field-types.ts:323-334), which re-screens with the SAME bypassed patternLooksCatastrophic (returns false), passes the REGEX_INPUT_CAP=2000 length check, then runs re.test(raw) and freezes the single Node event loop for tens of seconds — a denial of service for EVERY tenant on that API process, per request. The bulk import path (import-engine.ts:164 → validateRecord) runs the same pattern once per row, amplifying the freeze across an entire import. The input cap does not help because blowup is exponential and triggers far below 2000 chars. The code comments (field-types.ts:38-40,55-61,228-229) explicitly claim the exponential class is rejected and only polynomial residual remains — that claim is false.",
      "verified": "runtime-probed",
      "fix": "Treat ANY repetition quantifier after a group — including bounded `{n}` and `{n,m}` — as 'repeated' when screening bodyIsAmbiguous: change isOpenEndedBrace to also match exact `{n}` (n>=2) and bounded `{n,m}`, i.e. flag `(ambiguousBody){k}` for k>=2. Also flag a nested unbounded quantifier inside a fixed-repeat group. Add the bypass cases (`(a*){5}c`, `([a-z]*){8}$`, `(a|a){10}`, `(.*){10}`) to redos-guard.spec.ts as must-reject. Strongly recommended: move custom-field regex matching off the main event loop or onto a non-backtracking engine (RE2 / recheck), since heuristic scanners will keep missing shapes; at minimum run validateField's regex with a hard per-call time budget (worker thread / vm timeout).",
      "confidence": "high",
      "adversarial": {
        "real": true,
        "adjusted_severity": "high",
        "reason": "Independently confirmed. The guard treats a group as repeated only for *, +, or an OPEN-ENDED brace: hasAmbiguousRepeatedGroup at field-types.ts:126 uses `next === '{' && isOpenEndedBrace(p, i + 1)`, and isOpenEndedBrace at field-types.ts:134-135 is `/^\\{\\d*,\\d*\\}/.test(p.slice(at))` which REQUIRES a comma. So a fixed-count brace `(...){n}` (no comma) is never treated as repetition, and its body is never screened by bodyIsAmbiguous. I reimplemented the exact guard from this file: `(a*){5}c`, `([a-z]*){8}$`, `(a|a){10}`, `(.*){10}` all return false (PASS), while open-ended `(a+)+`/`(.*)*` are correctly rejected. The other gates do not catch it: length<200, no stacked `[*+][*+]`, and countUnbounded for `([a-z]*){8}$` is 1 (<=6). Runtime is genuinely exponential and triggers far below REGEX_INPUT_CAP=2000: in fresh node processes `([a-z]*){8}$` on inputs of 23/26/29/32/35 chars took ~1s/2.5s/5.8s/12s/24s (each +3 chars ~doubles), so a single ~35-char value freezes the shared event loop ~24s. The input cap (field-types.ts:329) is useless here. Call paths are real and reachable: both save paths gate only on patternSaveError->patternLooksCatastrophic (collections.service.ts:278,365; field-definitions.service.ts:262), and the match path validateField re-screens with the same bypassed function (field-types.ts:323) then runs re.test(raw) (field-types.ts:333) on every record write via dynamic-validator.ts:51 and once per row in bulk import (import-engine.ts:164). The spec (redos-guard.spec.ts) tests the comma forms `(a+){2,}` and `(.*a){11,}` but never a comma-less `(...){n}`, confirming this shape was missed; the code comments (lines 38-40,55-61,228-229) claiming the exponential class is rejected are false. Severity high rather than critical only because pattern authorship requires owner/admin role; but the blast radius is platform-wide (single Node event loop for all tenants) DoS triggerable per request by any user who supplies a ~40-char value for the field."
      }
    },
    {
      "severity": "info",
      "title": "OpenLibrary outbound fetch is not SSRF-exploitable",
      "file": "apps/api/src/catalog/openlibrary.ts:44",
      "evidence": "URL host is hardcoded `https://openlibrary.org/...` and the only injected value is `${isbn}` where isbn = digitsOnly(rawIsbn) (normalize.ts:42-46 strips everything except 0-9Xx) and must pass isValidIsbnShape (len 10 or 13). fetch uses `redirect: 'manual'` (openlibrary.ts:54) so a 3xx to a cloud-metadata/internal address is never chased, a 5s AbortController timeout (line 46), and readCappedJson enforces MAX_RESPONSE_BYTES=1_000_000 (lines 73,84,106).",
      "impact": "No attacker-controlled host/path; cannot reach internal addresses or metadata endpoints. Documented as verified-clean, not a vulnerability.",
      "verified": "static",
      "fix": "No change needed.",
      "confidence": "high"
    },
    {
      "severity": "info",
      "title": "Local storage driver and signed-URL ref handling block path traversal",
      "file": "apps/api/src/storage/drivers/local-driver.ts:111",
      "evidence": "safeResolve rejects null bytes, any `..` or empty path segment, and verifies `candidate === this.root || candidate.startsWith(this.root + sep)` (local-driver.ts:112-119); requireSafe throws EPERM otherwise. Stored filenames are server-generated (cuidLike + whitelisted extension, lines 55-57,134-153). The only user-facing ref entry (storage-demo.controller.ts:119 `ref = ${resourceType}/${filename}`) and the signed-download path (storage-demo.controller.ts:200 payload.ref) both flow through driver.get→safeResolve, and verify() cross-checks payload.tid against the resolved tenant (signed-url.service.ts:58-75).",
      "impact": "No path traversal / tenant-prefix escape via storage refs. Documented as verified-clean.",
      "verified": "static",
      "fix": "No change needed.",
      "confidence": "high"
    },
    {
      "severity": "info",
      "title": "No SQL injection, command injection, or XXE in raw SQL / pg_dump / XML parsing",
      "file": "apps/api/src/export/export-processors.ts:338",
      "evidence": "All $executeRaw/$queryRaw use Prisma tagged templates with ${} bound params (loans.service.ts:446-452,897-906; storage.service.ts:76-81,196-204; reservations.service.ts:343,515); $queryRawUnsafe sites use $1..$N placeholders + separate args (help.service.ts:79-95, effective-plan.service.ts:143-166) or a literal 'SELECT 1' (maintenance-processors.ts:149). pg_dump runs via execFile (array args, no shell) with credentials in PG* env not argv (export-processors.ts:327-344); readTables interpolates only table names sourced from pg_tables system catalog (export-processors.ts:373). MARCXML uses fast-xml-parser with no DTD/entity expansion (marc-parser.ts:142-149) → no XXE.",
      "impact": "No injection vector found across these surfaces. Documented as verified-clean.",
      "verified": "static",
      "fix": "No change needed.",
      "confidence": "high"
    }
  ]
}