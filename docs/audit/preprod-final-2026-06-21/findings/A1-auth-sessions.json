{
  "dimension": "A1-auth-sessions",
  "summary": "I read the full tenant auth surface (auth.controller/guard, login/signup/password-reset/email-verification services, jwt-session + session.middleware + cookie.service), the admin plane (admin-auth.*, admin-session, admin.middleware, admin-roles.guard), and the support/impersonation flow (impersonation middleware/session/cookie, support-key, support-session, mfa). The claimed-fixed items hold up well under real-code review: AUTH-01 session-epoch revocation is correctly keyed off an IMMUTABLE session start (`ist`), so sliding re-issues cannot launder a revoked session, and every tenant-user credential/disable path bumps `sessionsValidAfter` + busts the Redis cache (role-change relies on live DB re-read in RolesGuard/TenantGuard, which is sound). Reset/verify tokens are 32-byte CSPRNG with atomic GETDEL single-use; MFA uses replay-protected, fail-closed TOTP; DI uses explicit `@Inject(...)` / direct `controlDb` + `new Reflector()` so the prior tsx/Reflector 500 class of bug is avoided. The guards/middleware are correctly wired (RedisModule is @Global). The remaining issues are an account-lockout DoS (lockout is account-wide, not IP-scoped, and renewable), a couple of consistency gaps (admin-side `sessionsValidAfter` is read but never written; impersonation JWT skips the per-claim type checks the other verifiers do), and a weak staff first-login password floor. No auth bypass or cross-tenant breach found in this dimension. The live API was not running on :3001, so findings are static (Postgres/Redis are up but I did not mutate data).",
  "findings": [
    {
      "severity": "medium",
      "title": "Account-lockout DoS: login lockout is account-wide and renewable, not IP-scoped",
      "file": "apps/api/src/auth/login.service.ts:103-111,180-196",
      "evidence": "recordFailure() does `failedLogins: { increment: 1 }` then locks the ACCOUNT (`lockedUntil = now + loginLockoutMs`) once `failedLogins >= maxFailedLogins`. The lock is keyed only on the user row — every wrong password from ANY source IP advances the same counter. maxFailedLogins defaults to 5 and loginLockoutMs to 15 min (config/env.ts:304-305). The per-IP login budget is RL.loginPerIp = { limit: 20, windowSec: 300 } (auth.controller.ts:55), i.e. a single IP can submit 20 attempts / 5 min — comfortably more than the 5 needed to lock an account. There is no IP-scoping, progressive backoff, or CAPTCHA, so an attacker who knows (slug, email) can keep any account — including the owner — perpetually locked by sending 5 bad passwords every 15 minutes.",
      "impact": "Targeted denial of service against any known account (owners included). Bounded to 15-min windows but trivially renewable and cheap. Does not leak account existence (lockout returns the same generic error), but the availability impact is real and the prior AUTH-02 remediation only closed the enumeration oracle, not the DoS.",
      "verified": "static",
      "fix": "Don't hard-lock the account on raw failure count alone. Options: (1) scope the brute-force counter/lock to (account + IP) so one attacker can't lock a victim globally; (2) replace fixed account lock with exponential per-account delay + a CAPTCHA/proof-of-work step after N failures; (3) auto-clear the lock on a successful password+second-factor from a known device. At minimum, require the per-account lock to also consider source diversity so a single IP cannot lock an account it has never authenticated from.",
      "confidence": "high"
    },
    {
      "severity": "low",
      "title": "Admin-side session epoch (sessionsValidAfter) is read by the guard but never written anywhere",
      "file": "apps/api/src/admin/admin-auth.guard.ts:50-53",
      "evidence": "AdminAuthGuard reads admin.sessionsValidAfter and rejects tokens with `session.iat < validAfterMs/1000`. But a repo-wide grep for writes shows the column is never set: the only adminUser writes are recordFailure/recordSuccess (admin-auth.service.ts:85,91,104) and MFA enrolment (mfa.controller.ts:96) — none touch sessionsValidAfter. There is also no in-app admin password-change or admin-disable endpoint (admins are seed-managed), so the AUTH-01 'forced reset terminates existing admin sessions' guarantee documented in the guard comment (lines 21-23) cannot actually fire.",
      "impact": "Defense-in-depth that is effectively dead code today. Not exploitable now (a disabled admin is still caught by the status/disabledAt checks at lines 46-48, which run every request), but if an admin password-reset/rotation feature is added later without remembering to bump this column + nothing forces existing admin cookies (1h TTL) to die, the 'kill existing sessions on reset' expectation silently won't hold.",
      "verified": "static",
      "fix": "Either remove the unused branch to avoid a false sense of coverage, or — preferably before shipping any admin self-service password/credential change — wire that path to set adminUser.sessionsValidAfter = now() so the existing read becomes meaningful. Add a regression test asserting a post-reset admin cookie is rejected.",
      "confidence": "high"
    },
    {
      "severity": "low",
      "title": "Impersonation JWT verify trusts the decoded payload shape without per-claim type checks",
      "file": "apps/api/src/support/impersonation-session.service.ts:45-55",
      "evidence": "verify() does `jwt.verify(...) as ImpersonationPayload | undefined` and only checks `payload.imp === true`, then returns the cast object. The tenant and admin verifiers (jwt-session.service.ts:84-92, admin-session.service.ts:44-53) explicitly validate the type of every claim (sub/tid/role/iat/exp) per the AUTH-10 hardening — this one does not.",
      "impact": "Not exploitable in practice: the token is HMAC-signed with a dedicated impersonationSecret, and ImpersonationMiddleware (impersonation.middleware.ts:49-63) re-loads the SupportSession row by sessionId and cross-checks tenantId/adminId against the payload, so a malformed/forged payload is rejected downstream. It is, however, an inconsistency with the codebase's own AUTH-10 contract and would matter if any consumer ever trusts the payload before the DB cross-check.",
      "verified": "static",
      "fix": "Mirror the other two verifiers: validate typeof sub/adminId/tenantId/sessionId === 'string', imp === true, and iat/exp are numbers; return null otherwise instead of casting.",
      "confidence": "medium"
    },
    {
      "severity": "low",
      "title": "Staff first-login (complete-setup) allows a 4-character password",
      "file": "apps/api/src/auth/dto/login.dto.ts:44-46",
      "evidence": "CompleteSetupDto.newPassword is `@Length(4, 200)`, vs the 12-char minimum enforced on signup (signup.dto.ts:32) and password reset (password-reset.dto.ts:24). login.service.ts completeSetup() hashes whatever is supplied with no extra strength check.",
      "impact": "A staff member completing first-login setup can set a 4-char password, which is brute-forceable offline if the control-DB password hashes ever leak and is well below the floor applied everywhere else. Mitigated by per-account login lockout and that staff use usernames within one tenant, so it is low severity, but it's an inconsistent and weak floor for an interactive login credential.",
      "verified": "static",
      "fix": "Raise the complete-setup newPassword minimum to match the rest of the product (e.g. @MinLength(12)), or at least to a defensible interactive-login floor (>=8). If short staff PINs are intentional, document the threat model; otherwise align the DTO.",
      "confidence": "high"
    }
  ]
}