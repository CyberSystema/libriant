node_modules/
.next/
.turbo/
dist/
build/
coverage/
pnpm-lock.yaml
packages/db-control/prisma/migrations/
packages/db-tenant/prisma/migrations/
# Auto-generated by Next.js on every build — must not be hand-formatted.
apps/web/next-env.d.ts
# Audit deliverables: machine-generated findings JSON + reports, not source.
docs/audit/
# Generated: apps/site/dist is the production docroot, rendered by CI and
# baked into the edge image. Never commit it, never format it.
apps/site/dist/
# Generated byte-for-byte by `pnpm check:openapi --write` and compared
# byte-for-byte by the gate. Prettier collapses short JSON arrays onto one line,
# which would make the gate fail on a file nobody edited — so the generator, not
# prettier, is the formatter of record here.
docs/api/
# Committed format definitions. Their SHA-256 is recorded in digests.json and
# checked by `pnpm check:marc-schema`, so a reformat would fail a gate on a file
# nobody edited. The generator, not prettier, is the formatter of record.
packages/marc/src/definitions/
